import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gsb_ges_activite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
